@isTest
public class Ortho_ChartDatasetTest {
	@isTest
    public static void ortho_chartDatasetTest(){
        Ortho_ChartDataset dataset = new Ortho_ChartDataset();
        dataset.addDataValue(16.00);
        dataset.isEmpty();
    }
}