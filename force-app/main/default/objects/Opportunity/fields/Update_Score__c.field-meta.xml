<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Update_Score__c</fullName>
    <externalId>false</externalId>
    <formula>IF( CloseDate &lt;= Today() + 120,
IF(LastModifiedDate &gt; Now() - 30,4, 
 IF(LastModifiedDate &gt; Now() - 40,3, 
  IF(LastModifiedDate &gt; Now() - 50,2, 
   IF(LastModifiedDate &gt; Now() - 60,1,
    IF(LastModifiedDate &gt; Now() - 70, 0, 0)
   )
  )
 )
), 4)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Update Score</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
